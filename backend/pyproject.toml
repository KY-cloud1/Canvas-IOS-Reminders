[project]
name = "AssignmentBridge"
version = "0.1.0"
dependencies = [
    "fastapi",
    "keyring",
    "ngrok>=1.7.0",
    "playwright>=1.40.0",
    "python-dotenv",
    "sse-starlette",
    "uvicorn",
]
